---
- name: Install base packages for Ubuntu
  become: true
  ansible.builtin.apt:
    name: "{{ ubuntu_base_packages }}"
    state: present
  when:
    - ubuntu_base_packages | length > 0
    - ansible_facts['distribution'] == "Ubuntu"
