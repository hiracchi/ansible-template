collections:
  - name: ansible.posix
    version: ">=1.5.0"
  # community.general.sudoers の defaults パラメータ(sudoログ出力先の指定などに使用)は
  # community.general 13.1.0 で追加されたため、それ以上を要求する
  - name: community.general
    version: ">=13.1.0"
